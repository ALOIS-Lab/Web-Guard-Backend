PORT=
JWT_SECRET=
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
# Optional admin inbox — only used when ALERT_CC_ADMIN=true
ALERT_EMAIL=
ALERT_CC_ADMIN=
# Minutes between repeat emails while a site stays down (default 30)
ALERT_COOLDOWN_MIN=
CLIENT_URL=
# Optional — enables generative AI replies for /api/support/chat
OPENAI_API_KEY=
OPENAI_MODEL=
# Optional — PageSpeed Insights (higher quota). Works without a key at low volume.
PAGESPEED_API_KEY=
